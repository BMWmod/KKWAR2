{actor
	(include "/properties/missile_impulse.ext")
	("proximity")
	("guidance_common" 
		missile(rvv-md) 
		stuff(rvv-md_impactor) 
		country(rus) type(ir) 
		calibre(170) length(2920)
		speed(fast) guidance(propsam))
	("maneuvrability_SAMs_short_impulse" deg_per_s(53))
}

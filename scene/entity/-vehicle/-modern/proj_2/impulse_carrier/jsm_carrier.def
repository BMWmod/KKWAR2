{actor
	(include "/properties/missile_impulse.ext")
	("antiship_missile"
		missile(jsm) 
		stuff(jsm_impactor) 
		country(usa) type(antiship) 
		calibre(700) length(3900)
		speed(slow) guidance()
	)

	("maneuvrability_SAM_impulse" deg_per_s(35))
	("cwps_rangefinder")
	("targetable_missile")
	{targetSelector "corr"}
	{props "ignore_linked_pointer"}
;	{Weaponry
;		{place "targeting_sys_jsm"
;			{weapon "missilets_propslow" filling "usa_active_aj cwps2 ammo" 200}
;			{foresight "missle_foresight"}
;			{charger "driver"}
;			{gunner "driver"}
;			{basic}
;			{SequentialAim 0}
;		}
;	}
}
